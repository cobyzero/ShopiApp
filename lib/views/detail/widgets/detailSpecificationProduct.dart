import 'package:flutter/material.dart';

class DetailSpecificationProduct extends StatelessWidget {
  const DetailSpecificationProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        child: Column(
          children: [Text("Choose your new Macbook Pro")],
        ),
      ),
    );
  }
}
